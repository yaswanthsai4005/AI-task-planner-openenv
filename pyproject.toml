[project]
name = "ai-task-planner"
version = "0.1.0"
dependencies = [
  "openenv-core>=0.2.0",
  "openai",
  "pydantic"
]
authors = [{name = "Yash"}]

[project.scripts]
server = "server.app:main"

[tool.openenv]
entrypoint = "environment:TaskEnv"

